{
  "name": "@via/server",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"tsc --watch\" \"nodemon dist/server.js\"",
    "build": "tsc",
    "clean": "rm -rf .turbo dist node_modules",
    "test": "vitest"
  },
  "exports": {
    "./app-router": {
      "types": "./src/router.ts"
    }
  },
  "dependencies": {
    "@via/router": "workspace:*",
    "@via/store": "workspace:*",
    "@via/video-core": "workspace:*",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "multer": "1.4.5-lts.1",
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "@trpc/server": "^10.45.2",
    "@types/cors": "^2.8.17",
    "@types/multer": "^1.4.11",
    "@types/uuid": "^10.0.0",
    "@via/node-sdk": "workspace:*",
    "@via/typescript-config": "workspace:*",
    "dotenv": "^16.4.5",
    "vitest": "^1.6.0"
  }
}
