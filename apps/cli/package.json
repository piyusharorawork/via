{
  "name": "@via/cli",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf .turbo dist node_modules exports",
    "start": "tsc; node dist/cli.js",
    "download-video": "tsc; node dist/download-video.cli.js"
  },
  "devDependencies": {
    "@via/typescript-config": "workspace:*",
    "typescript": "latest"
  },
  "dependencies": {
    "@via/editly": "workspace:*",
    "@via/video-core": "workspace:*",
    "@via/youtube-downloader": "workspace:*",
    "@via/store": "workspace:*"
  }
}
