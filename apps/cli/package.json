{
  "name": "@via/cli",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf .turbo dist node_modules exports downloads temp",
    "start": "tsc; node dist/cli.js",
    "load-videos": "tsc; node dist/load-videos.js"
  },
  "devDependencies": {
    "@types/uuid": "^10.0.0",
    "@via/typescript-config": "workspace:*",
    "typescript": "latest"
  },
  "dependencies": {
    "@trpc/client": "^10.45.2",
    "@via/core": "workspace:*",
    "@via/editly": "workspace:*",
    "@via/server": "workspace:*",
    "@via/store": "workspace:*",
    "@via/video-core": "workspace:*",
    "node-fetch": "^3.3.2",
    "uuid": "^10.0.0",
    "@via/machine": "workspace:*"
  }
}
