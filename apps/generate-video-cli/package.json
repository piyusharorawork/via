{
  "name": "@via/generate-video-cli",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf .turbo dist node_modules exports",
    "start": "tsc; node dist/index.js"
  },
  "devDependencies": {
    "@via/typescript-config": "workspace:*",
    "typescript": "latest"
  },
  "dependencies": {
    "@via/editly": "workspace:*"
  }
}
