{
  "name": "@via/machine",
  "type": "module",
  "scripts": {
    "compile": "tsc --watch",
    "build": "tsc",
    "clean": "rm -rf .turbo dist node_modules",
    "test:watch": "vitest"
  },
  "exports": {
    "./video-management-machine": {
      "types": "./src/video-management-machine.ts",
      "default": "./dist/video-management-machine.js"
    }
  },
  "devDependencies": {
    "@via/typescript-config": "workspace:*",
    "ts-node": "^10.9.2",
    "typescript": "latest",
    "vitest": "^1.6.0"
  },
  "dependencies": {
    "@trpc/client": "^10.45.2",
    "@via/core": "workspace:*",
    "@via/server": "workspace:*",
    "node-fetch": "^3.3.2",
    "xstate": "^5.15.0"
  }
}
