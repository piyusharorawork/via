{
  "name": "@via/router",
  "type": "module",
  "scripts": {
    "dev": "tsc --watch",
    "build": "tsc",
    "clean": "rm -rf .turbo dist node_modules",
    "test": "vitest"
  },
  "exports": {
    "./router": {
      "types": "./src/router.ts",
      "default": "./dist/router.js"
    }
  },
  "devDependencies": {
    "@via/typescript-config": "workspace:*",
    "typescript": "latest",
    "vitest": "^1.6.0"
  },
  "dependencies": {
    "@trpc/server": "^10.45.2",
    "zod": "^3.23.8"
  }
}
