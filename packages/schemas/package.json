{
  "name": "@via/schemas",
  "type": "module",
  "scripts": {
    "dev": "tsc --watch",
    "build": "tsc",
    "clean": "rm -rf .turbo dist node_modules",
    "test": "vitest"
  },
  "exports": {
    "./schema": {
      "types": "./src/schema.ts",
      "default": "./dist/schema.js"
    }
  },
  "devDependencies": {
    "@via/typescript-config": "workspace:*",
    "typescript": "latest",
    "vitest": "^1.6.0"
  },
  "dependencies": {
    "zod": "^3.23.8"
  }
}
