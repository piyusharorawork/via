{
  "name": "extension",
  "displayName": "via",
  "description": "",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.98.0"
  },
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "via.show-clip-info",
        "title": "show-clip-info"
      }
    ]
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/moment": "^2.13.0",
    "@types/node": "20.x",
    "@types/vscode": "^1.98.0",
    "typescript": "^5.8.2"
  },
  "dependencies": {
    "moment": "^2.30.1"
  }
}
